study c++, python, linux, ros. 
git init
git status
git add .
git add file1.txt
git commit -m 'v1.0'
git log
git reset --hard 版本号
git reflog
git checkout -- 文件名   相当于撤回所有修改
touch file2.txt  新增文件

cd e:/
ls
mkdir 文件名